<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uploaded Images</title>
</head>
<body>
    <h1>Uploaded Images</h1>
    <% if (items.length > 0) { %>
        <% items.forEach(item => { %>
            <div>
                <img src="data:<%= item.img.contentType %>;base64,<%= item.img.data.toString('base64') %>" width="300" />
            </div>
        <% }); %>
    <% } else { %>
        <p>No images uploaded yet.</p>
    <% } %>
</body>
</html>
